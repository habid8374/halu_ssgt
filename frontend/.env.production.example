# Producción sin dominio propio: URLs explícitas hacia el backend de Railway.
# (Cuando tengas dominio, se cambia a derivación por hostname multi-tenant.)
NEXT_PUBLIC_API_URL=https://TU-BACKEND.up.railway.app/api
NEXT_PUBLIC_WS_URL=wss://TU-BACKEND.up.railway.app/ws
