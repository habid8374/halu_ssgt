from django.apps import AppConfig


class AccidentesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.accidentes"
    verbose_name = "Accidentes de trabajo (fase 2)"
